info:
  name: price-stats
  type: http

http:
  method: GET
  url: http://localhost:8000/api/tors/price-stats?groupBy=category
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
