info:
  name: detail
  type: http

http:
  method: GET
  url: http://localhost:8000/api/tors/:id
  auth: inherit

params:
  path:
    - name: id
      value: "REPLACE_WITH_TOR_ID"
      enabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
