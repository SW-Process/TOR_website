info:
  name: list-runs
  type: http

http:
  method: GET
  url: http://localhost:8000/api/ingestion/runs?limit=20
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
